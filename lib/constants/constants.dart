class Constants {
  // TODO: change to the real API IP when it's available
  // 10.0.2.2 corresponds to localhost on a Android VM
  static const String baseApiUrl = "splitit.lab64.eu";
  static const int basePort = 0;
  static const String baseScheme = "https";
}
